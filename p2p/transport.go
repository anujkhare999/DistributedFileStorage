package p2p

//Peer is an interface that represent the remote node
type Peer interface {

}

// Transport is anything that handles the communication between the node in the network
// This can be of form TCP, UDP , Websockets,... )
type Transport interface {

}